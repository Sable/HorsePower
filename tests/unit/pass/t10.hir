// 10 : udchain A - build chain

module udchain {
    //global NEW:i32 = 1:i32;
    def main() {
        x0:i32 = (20,30,40):i32;
        y0:i32 = @add(x0, x0);
        z0:i32 = @mul(x0, y0);
        m0:bool = @lt(x0,1:i32);
        if(z0){
            m0 = @gt(x0,1:i32);
        }
        m1:i32 = @add(m0, m0);
        //m1:i32 = @add(m0, NEW);
    }
}

